// 占位 smoke test——真实用例见 models_test.dart / importer_test.dart /
// schedule_service_test.dart。保留本文件以免默认计数器测试在 main.dart 改动后编译失败。
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
